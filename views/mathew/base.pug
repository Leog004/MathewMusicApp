doctype html
html
    head
        meta(charset='utf-8')
        meta(name='viewport' content='width=device-width, initial-scale=1, shrink-to-fit=no')
        meta(name='description' content='Mathew Music')
        meta(name='keywords' content='Music')
        
        title #{Title}
        <link href="https://fonts.googleapis.com/css?family=Oswald:300,400%7CKarla:400,700" rel="stylesheet">

        link(rel='shortcut icon' href='/favicon/favicon.ico')
        link(rel='apple-touch-icon' sizes='180x180' href='/favicon/apple-touch-icon.png')
        link(rel='icon' type='image/png' sizes='32x32' href='/favicon/favicon-32x32.png')
        link(rel='icon' type='image/png' sizes='16x16' href='/favicon/favicon-16x16.png')

        link(rel='stylesheet' href='/css/bootstrap-reboot.css')
        link(rel='stylesheet' href='/css/bootstrap-grid.css')
        link(rel='stylesheet' href='/css/material-design-iconic-font.css')
        link(rel='stylesheet' href='/css/style.css')

        meta(property='og:title' content='Mathew Music')
        meta(property='og:image' content=getMetaData.photoLink.url)
        block head
            style#castilo-inline-style.
                .alert {
                    position: fixed;
                    top: 0;
                    left: 50%;
                    -webkit-transform: translateX(-50%);
                    transform: translateX(-50%);
                    z-index: 9999;
                    color: #fff;
                    font-size: 1.8rem;
                    font-weight: 400;
                    text-align: center;
                    border-bottom-left-radius: 5px;
                    border-bottom-right-radius: 5px;
                    padding: 1.6rem 15rem;
                    -webkit-box-shadow: 0 2rem 4rem rgba(0, 0, 0, 0.25);
                    box-shadow: 0 2rem 4rem rgba(0, 0, 0, 0.25);
                }
                .alert--success {
                    background-color: #20bf6b;
                }
                .alert--error {
                    background-color: #eb4d4b;
                }                
        

body.home
include _header
block content
include _footer

script(src='/js/jquery-3.2.1.min.js')
script(src='/js/modernizr-custom.js')
script(src='/js/functions.js')
script.
        document.addEventListener('DOMContentLoaded', function() {
            var ip = '000.000.000';

            $.get("https://ipinfo.io", function(response) {
                ip = response.ip;
                
                $.post("/api/v1/analytics/", {ipAddress : ip}, function(data, status){
                    console.log(`Data: ${JSON.stringify(data)} Status:${status}`);
                }); 

            }, "jsonp");

           

        }, false);


block javascript



  